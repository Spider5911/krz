package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Brand Accents (Clean Minimalism uses iOS Blue as primary accent)
val WhatsAppGreenLight = Color(0xFF34C759)
val WhatsAppGreenDark = Color(0xFF30D158)
val WhatsAppBlue = Color(0xFF007AFF) // Clean Minimalism brand accent

// Light Theme Colors (Clean Slate & Minimalist)
val iOSLightBackground = Color(0xFFFFFFFF) // Pure White Background
val iOSLightSurface = Color(0xFFF8FAFC)    // Light slate-50 background cards
val iOSLightTextPrimary = Color(0xFF0F172A) // slate-900 for modern premium text
val iOSLightTextSecondary = Color(0xFF64748B) // slate-500
val iOSLightBubbleSender = Color(0xFFE2F0FD) // Minimalist soft light blue
val iOSLightBubbleReceiver = Color(0xFFF1F5F9) // Minimalist soft gray (slate-100)

// Dark Theme Colors (Deep space slate)
val iOSDarkBackground = Color(0xFF0F172A) // Slate-900 dark background
val iOSDarkSurface = Color(0xFF1E293B)    // Slate-800 surfaces
val iOSDarkTextPrimary = Color(0xFFF8FAFC) // Slate-50 high contrast text
val iOSDarkTextSecondary = Color(0xFF94A3B8) // Slate-400
val iOSDarkBubbleSender = Color(0xFF0369A1) // Sky-700
val iOSDarkBubbleReceiver = Color(0xFF334155) // Slate-700

